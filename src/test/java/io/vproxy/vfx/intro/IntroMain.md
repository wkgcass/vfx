# IntroMain.java vs IntroMain.ts
## Summary
Intro main class that sets up the demo. Java uses `Application.launch()`. TS exports a `mainIntro()` async function that sets the theme, installs the FontProvider, and calls `await app.start()`.
## Verdict
CONSISTENT.
