# LoadingItem.java vs LoadingItem.ts

## Summary
Data class representing a loading task with weight, name, and load function. Both the `Runnable` and boolean-returning load function overloads are preserved.

## Verdict
CONSISTENT — same behavior.
